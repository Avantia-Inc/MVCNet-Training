@model MvcTraining.Web.Areas.Admin.Models.Product

Edit Product
@using (Ajax.BeginForm("Edit", new AjaxOptions() { HttpMethod = "Post", OnSuccess = "SaveSuccess", OnFailure = "SaveError" }))
{
<table border="1">
        <tr>
            <td>Name</td>
            <td>@Html.EditorFor(model => model.Name)</td>
        </tr>
        <tr>
            <td>Category</td>
            <td>@Html.EditorFor(model => model.Category)</td>
        </tr>
        <tr>
            <td>Description</td>
            <td>@Html.EditorFor(model => model.Description)</td>
        </tr>
        <tr>
            <td>Weight</td>
            <td>@Html.EditorFor(model => model.Weight)</td>
        </tr>
    </table>    <button>Save</button>
}
@{ string prompt = "OK to delete " + Model.Name + "?";}

@using (Ajax.BeginForm("Delete", new AjaxOptions() { HttpMethod = "Post", Confirm = prompt, OnSuccess = "Deleted", OnFailure = "DeleteFailed" }))
{
    <input type="hidden" name="id" value="@Model.Id" />
    <button>Delete</button>
}